# ============================================================================
#  RAG 知识库问答系统 - Python 依赖清单 (V1.0)
#  安装方式：pip install -r requirements.txt
# ============================================================================

# --- LangChain 核心 ---
langchain>=0.3.0
langchain-community>=0.3.0
langchain-text-splitters>=0.3.0
langchain-openai>=0.2.0    # ChatOpenAI（多轮对话 LCEL 链用）
langchain-ollama>=0.2.0    # ChatOllama

# --- Web 框架 ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic>=2.0.0
streamlit>=1.30.0  # 前端界面

# --- 向量数据库 ---
pgvector>=0.3.0
psycopg[binary]>=3.2.0
chromadb>=0.5.0  # 开发环境 fallback

# --- 嵌入 & 重排模型 ---
sentence-transformers>=3.0.0
FlagEmbedding>=1.3.0

# --- 检索 ---
rank-bm25>=0.2.2
jieba>=0.42.1

# --- PDF / Word / Markdown 处理 ---
pypdf>=5.0.0
python-docx>=1.1.0
unstructured>=0.15.0

# --- OCR 识别（PDF 图片/扫描件）---
# 可选：不安装则 OCR 功能自动降级跳过，不影响其他功能
# Python <3.13 也可用 rapidocr-onnxruntime（1.x，等价功能）
rapidocr>=2.0.0
PyMuPDF>=1.24.0  # 仅用于 PDF 页渲染与图片提取

# --- LLM API 客户端 ---
openai>=1.50.0       # DeepSeek / Qwen 兼容 OpenAI API
ollama>=0.4.0        # Ollama 本地模型

# --- Web 搜索 ---
duckduckgo-search>=6.0.0
httpx>=0.28.0

# --- Markdown 解析 ---
markdown>=3.4.0

# --- 工具 ---
python-dotenv>=1.0.0
